# Trail 截图对比网页 QA

任务：把已有 iOS 改版证据整理为可浏览的临时网页；不重绘 App，不创建新一轮 App 视觉方案。

## 视觉来源与实现

- source visual truth path: `../before/10-replay.png`、`../after/10-replay.png`，以及 `src/content.json` 引用的原始界面截图。
- implementation URL: http://127.0.0.1:4178/
- Browser: Chrome 后备浏览器；CUA 返回 `Browser is not available: iab` 后采用。复用 tab `1776952068`。
- viewport: 1728 × 1000 CSS px。
- 原始回放图：1206 × 2622 px，对应 402 × 874 pt，3 倍密度。
- 网页使用原始图片字节，70 处图片引用全部 SHA-256 一致；网页以 contain 等比例缩小显示，点击放大可查看原图。页面周围的浏览导航属于对比工具，不是 iOS 屏幕的一部分。
- state: 默认回放前后并排、只看调整后、终端会话、放大细节、设置目录、功能取舍表。
- 调整前回放属于搜索状态，调整后属于普通播放；页面显式提示，未宣称两图交互状态一致。

## 浏览器交互核对

由用户指定的 Luna 子代理执行 CUA：

- 默认前后图片正常加载。
- 只看调整后 / 并排切换正常。
- 终端会话、设置目录切换正常。
- 图片放大、关闭正常；放大图清晰。
- 功能取舍长表可滚动，中文未见截断。
- 浏览器错误与警告日志为空。
- 原图 SHA 验证和构建通过是补充检查，未代替浏览器检查。

## 截图与归档

implementation screenshot evidence: Luna 子代理 `/root/simulator_ui` 的 CUA 工具图像输出，Chrome tab `1776952068`。截图由工具直接展示，未提供可引用的落盘路径；没有伪造本地截图文件。

- 默认并排截图：Uint8Array，71,189 bytes；浏览器 viewport 为 1728 × 1000 CSS px。工具未单独报告 PNG 像素密度，不据此做像素级误差主张。
- 放大细节截图：Uint8Array，39,040 bytes。用于核对终端文字与播放按钮等小尺寸内容。
- Full-view comparison：默认网页在同一画面显示原始 before / after 图，原图字节校验一致；对比工具未重绘或裁剪图片。
- Focused comparison：放大弹窗进一步确认原图字体与控制条清晰；图片仍为同一源文件。
- 最终默认回放并排页面已恢复，弹窗关闭，浏览器保持打开。
- 窄屏人工测试未执行：当前 CUA 公开 API 未提供 viewport 调整。作为验证范围限制保留，不把代码中的响应式样式当作已完成的窄屏验证。

## 检查记录

- 字体：原图内字体未改动；网页系统中文字体，标题、操作和说明层级清楚。
- 布局：前后图等比例显示，网页画布与图片不混淆；桌面未见溢出。
- 颜色：原图颜色保持，网页中性背景与蓝色选中状态分离。
- 图片：未裁剪、未重绘；原图精细内容可通过放大查看。
- 文案：38 个场景与 22 项功能取舍均来自既有说明；无前图场景标为补充，通用组件与系统截图范围单独注明。

## 比较历史

首次浏览器检查未发现功能阻塞或布局溢出。检查前修正了无前图场景的模式选中状态，以及跳到主内容的 hash 行为。

Luna 提到宽屏下手机图片周围留白较大，并说明不影响操作或理解。主代理按任务范围评定为 P3：使用等比例全图展示保证手机首尾均可见，原图放大入口已验证。它不是图片裁剪、主体内容缺失或持久控件不可达，无需为临时对比工具改动图片比例。没有待修复的 P0/P1/P2 项。

final result: passed

适用范围：通过桌面浏览器检查；窄屏人工核验、截图文件归档未完成，不影响已核对的本机桌面对比流程。没有宣称真机安装或实际服务验证已完成。


## 2026-09-11 · 第3张紧凑尺度更新

增加13个本轮场景，保留38个旧场景；51场景/96个图像引用检查通过，最终原生截图副本SHA256匹配。Luna在Chrome tab1776952068复核SSH并排、放大/关闭、选定稿查看/关闭、回放与设置，图像完整，中文未裁切；公开错误/警告日志为空。原生实现与选定稿的对比、比例、3项P2修复及边界见`../../ios-density-20260911/design-qa.md`。

final result: passed
