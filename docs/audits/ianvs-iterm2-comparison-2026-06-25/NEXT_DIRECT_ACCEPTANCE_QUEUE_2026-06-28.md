# Ianvs Terminal Next Direct Acceptance Queue

Date: 2026-06-28

Scope: remaining work from the seven re-sorted buckets that can be started without new target hardware, cross-platform hosts, quiet-host performance conditions, or a product-scope decision. Each item must close with automated evidence and may add a local desktop or Computer Use smoke only as supporting evidence.

## Recommended Next Batch

| Priority | Bucket | Item | Why it can proceed now | Acceptance evidence |
| --- | --- | --- | --- | --- |
| 1 | 7. UX optimization follow-ups | Profile editor search inside settings | Completed 2026-06-28. Profile editor section navigation now includes a local settings search that filters matching sections and jumps to them without dirtying the profile form. | `cd example && flutter test test/profiles/profile_editor_test.dart --plain-name "profile editor search filters and jumps to matching sections"` passed; `cd example && flutter test test/profiles test/shell/shell_screen_phase3_test.dart --plain-name "profile"` passed, 42 tests; `cd example && flutter analyze lib/features/profiles test/profiles` passed. |
| 2 | 7. UX optimization follow-ups | Profile editor dirty-state summary plus per-section reset/revert | Completed 2026-06-28. The editor now summarizes modified sections in the section rail, marks modified section entries, and can reset an individual section back to the initial profile snapshot. | `cd example && flutter test test/profiles/profile_editor_test.dart --plain-name "profile editor summarizes and resets dirty sections"` passed; `cd example && flutter test test/profiles test/shell/shell_screen_phase3_test.dart --plain-name "profile"` passed, 43 tests; `cd example && flutter analyze lib/features/profiles test/profiles` passed. |
| 3 | 7. UX optimization follow-ups | Dynamic profile import preview and conflict handling | Completed 2026-06-28. Dynamic Profiles now parses into a preview before import, shows new/replacement counts, marks existing-profile replacements, and reports added/replaced counts back to the shell import flow. | `cd example && flutter test test/profiles/profile_sheets_test.dart --plain-name "dynamic profiles"` passed; `cd example && flutter test test/widget_test.dart --plain-name "dynamic profiles imports iTerm profile JSON"` passed; `cd example && flutter test test/profiles test/shell/shell_screen_phase3_test.dart --plain-name "profile"` passed, 44 tests; `cd example && flutter analyze lib/features/profiles lib/features/shell test/profiles test/widget_test.dart` passed. |
| 4 | 7. UX optimization follow-ups | Ianvs profile/theme export | Completed 2026-06-28 for deterministic repository export. Profile documents can now be exported through `ProfileRepository.exportDocument(...)` with safe filenames; theme preset export was already covered by `LocalTerminalThemeRepository.exportPreset(...)`. Native save-dialog UI remains out of this repository-level slice. | `cd example && flutter test test/profiles/profile_repository_test.dart test/visual/local_terminal_theme_repository_test.dart` passed, 24 tests; `cd example && flutter analyze lib/features/profiles test/profiles test/visual/local_terminal_theme_repository_test.dart` passed. |
| 5 | 7. UX optimization follow-ups | Optional tab color support | Completed 2026-06-28. Profiles now support optional `appearance.colors.special.tab` values, Dynamic Profiles imports iTerm-style `Tab Color`, the profile editor can edit/reset the field, and the tab strip/overflow selector render stable tab-color markers. | `flutter test packages/ianvs_terminal/test/terminal_config_test.dart` passed, 18 tests; `cd example && flutter test test/profiles/profile_repository_test.dart test/profiles/profile_editor_test.dart` passed, 32 tests; `cd example && flutter test test/widget_test.dart --plain-name "tab"` passed, 23 tests; `flutter analyze packages/ianvs_terminal/lib/src/config/terminal_config.dart packages/ianvs_terminal/test/terminal_config_test.dart example/lib/features/profiles example/lib/features/shell example/lib/ui/foundation/terminal_theme_presets.dart example/test/profiles example/test/widget_test.dart` passed. |
| 6 | 7. UX optimization follow-ups / 4. Roadmap local workspace | Shell integration health visible in status bar or pane header | Completed 2026-06-28. The status bar now renders `SHELL WAITING`, `SHELL PARTIAL`, and `SHELL ACTIVE` from the active pane's shell integration snapshot, with tooltips and semantics labels. | `cd example && flutter test test/widget_test.dart --plain-name "shell integration"` passed, 6 tests; `cd example && flutter test test/widget_test.dart --plain-name "shell status"` passed, 3 tests; `cd example && flutter analyze lib/features/shell test/widget_test.dart test/shell/instant_replay_store_test.dart test/shell/shell_screen_phase4_test.dart` passed. |
| 7 | 7. UX optimization follow-ups | Setup-guided empty states for annotations and captured output | Completed 2026-06-28. Both sheets now show setup-guided empty states with stable keys and step rows instead of a single passive empty sentence. | `cd example && flutter test test/widget_test.dart --plain-name "annotations"` passed, 2 tests; `cd example && flutter test test/widget_test.dart --plain-name "captured output"` passed, 3 tests. |
| 8 | 7. UX optimization follow-ups | Instant replay timeline and retention policy display | Completed 2026-06-28. The existing timeline/marker UI is covered by the focused replay workspace test, and the controls now expose the active retention policy (`Retains latest 60 frames`) from `InstantReplayStore.frameLimit`. | `cd example && flutter test test/widget_test.dart --plain-name "instant replay"` passed; `cd example && flutter test test/shell/instant_replay_store_test.dart` passed, 13 tests. |
| 9 | 2. Environment / 7. UX follow-ups | Hotkey-window failure UI mapping | Completed 2026-06-28 from current implementation. The command menu surfaces unregistered shortcut/error-code diagnostics and blocks the bridge toggle when registration is missing. Native global hotkey ownership remains out of scope. | `cd example && flutter test test/shell/shell_screen_phase4_test.dart --plain-name "hotkey"` passed; `cd example && flutter test test/widget_test.dart --plain-name "hotkey"` passed. |
| 10 | 2. Environment / 7. UX follow-ups | Notification permission diagnostics UI | Completed 2026-06-28 from current implementation. Authorization failures show actionable snackbar feedback, command menu notification rows surface the blocked-system state, and the warning clears after a successful send. No claim about native banner delivery. | `cd example && flutter test test/widget_test.dart --plain-name "notification"` passed, 7 tests. |
| 11 | 7. UX optimization follow-ups | Keyboard-only focus traversal for tabs, panes, command menu, search, profile editor, and Toolbelt | Partially completed 2026-06-28 for the command menu and Toolbelt slices. The command menu now uses ordered focus traversal so the search field tabs into action tiles before the close button, and Toolbelt tabs now expose an ordered keyboard path with a Tab/Enter widget regression. The full tabs/panes/profile editor sweep remains open. | `cd example && flutter test test/widget_test.dart --plain-name "keyboard-only focus traversal"` passed; `cd example && flutter test test/widget_test.dart --plain-name toolbelt` passed, 3 tests. |
| 12 | 2. Environment | Focused Flutter semantics regression for command menu/search/profile/toolbelt | Partially completed 2026-06-28 for the shell command menu/search and Toolbelt slices. Focused widget regressions now verify selected-tab button semantics, command-menu search text-field semantics, and Toolbelt panel/tab semantics. This does not prove the host `AXTree` log issue, and profile focused semantics remain open. | `cd example && flutter test test/widget_test.dart --plain-name "focused shell semantics"` passed; `cd example && flutter test test/widget_test.dart --plain-name toolbelt` passed, 3 tests; keep host AXTree proof open. |

## Not Direct In This Host

| Bucket | Item | Reason |
| --- | --- | --- |
| 1 / 4 | Quiet-host or cross-machine performance baseline | Requires a quiet machine and comparable host metadata. |
| 2 | Foreground ownership / Flutter `open returned 1` cleanup | Current Codex host cannot prove frontmost ownership; standard desktop host proof is required. |
| 2 | Full accessibility-tree stability / `AXTree` bridge log elimination | Needs host-level reproduction and accessibility tooling beyond widget semantics. |
| 3 / 6 | Fractional-display text sharpness | Geometry snapping is automated, but real 125%/150% display sharpness needs matching display scale. |
| 3 / 6 | Windows Ctrl-letter/CapsLock and Android physical-keyboard duplication | Requires target OS/hardware. |
| 3 / 6 | Kitty keyboard protocol implementation | Still a product-scope decision; default xterm behavior is already protected. |
| 4 | M3 row-range annotation renderer implementation | The design is ready, but roadmap sequencing says to start M3 after evidence-lane closure; treat this as next-phase, not immediate cleanup. |
| 5 | Branch-only product line | Removed from `main`; no current-branch work. |

## Suggested Execution Order

Optional tab color is closed with schema, editor, Dynamic Profiles import, tab strip, overflow, and automated proof. Continue the broader keyboard traversal and Flutter semantics sweeps for tabs, panes, and profile editor as focused automated slices; keep host AXTree proof separate.
