# AI Learning Log — 2026-04-12 to 2026-05-12

## Sources reviewed

- month-level Codex work report covering 304 sessions and 1996 user inputs.
- `AGENTS.md`, `docs/ARCHITECTURE.md`, `docs/TESTING.md`, and `docs/README.md` — existing guidance checked before promotion.
- `docs/ai/learning-log/2026-05-11.md` — daily curation already written for the terminal theme work.

## Durable updates made

- Target: none
- Change: no AGENTS or skill promotion.
- Evidence: the strongest durable lessons are already captured by `docs/ARCHITECTURE.md` and `docs/TESTING.md`; the remaining items are feature-specific learning-log material.

## Candidates logged but not promoted

- Candidate: keep `packages/flutterm_pty` focused on low-level PTY transport, `packages/flutterm_terminal` focused on terminal runtime/view contracts, and `example/` responsible for app shell, profiles, tabs, menus, and product defaults.
- Reason not promoted: already documented in `docs/ARCHITECTURE.md`.
- Evidence: late-April workspace/package split, code reviews, and migration tasks converged on this boundary.

- Candidate: tests and example code should use public package APIs such as `ptySessionBackendProvider`, not private native/core implementation imports.
- Reason not promoted: captured by the architecture boundary and handled in targeted tests.
- Evidence: migration tasks updated widget/session controller tests to public APIs.

- Candidate: terminal rendering changes need layered verification: package tests, example tests, native/core checks, and GUI/manual matrix when behavior depends on real macOS input, trackpad, or PTY.
- Reason not promoted: already documented in `docs/TESTING.md`.
- Evidence: many terminal sessions used targeted tests first, then full verification for selection, scrollback, VT behavior, and GUI paths.

- Candidate: for terminal themes, use explicit special colors plus ANSI normal/bright palettes; do not infer the whole palette from one accent color.
- Reason not promoted: logged in the existing 2026-05-11 learning log and implemented in the feature branch.
- Evidence: terminal theme planning and implementation moved from four colors to grouped palette slots.

- Candidate: commit can succeed while push fails if GitHub HTTPS/SSH credentials are unavailable.
- Reason not promoted: environment-specific.
- Evidence: final theme commit was local only because both HTTPS and SSH push paths lacked credentials.

## Conflicts

- Conflict: none
- Existing guidance:
- Candidate guidance:
- Recommended resolution:

## Skipped sensitive or unsafe items

- Item category: none promoted; local repo paths kept only as generic context.
- Reason skipped:

## Validation

- Commands run: redaction helper on this log; `git diff --check`; trailing-whitespace scan; targeted sensitive-pattern scan.
- Result: no redaction changes; `git diff --check` produced no output; no trailing whitespace or targeted sensitive-pattern matches.
