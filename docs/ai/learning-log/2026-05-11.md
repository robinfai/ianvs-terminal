# AI Learning Log — 2026-05-11

## Sources reviewed

- `rollout-2026-05-11T12-01-20-019e1532-52ee-7bd0-8d9d-62ae4f5ab620.jsonl` — merge conflict repair around profile models, built-in shell compatibility, and session event exhaustiveness.
- `rollout-2026-05-11T12-11-45-019e153b-ddff-75b3-8966-4b798afb1e0e.jsonl` — terminal color theme design, package-level palette model, native application path, tests, commit, and failed push due to missing credentials.

## Durable updates made

- Target: none
- Change: no AGENTS or skill promotion
- Evidence: the lessons are useful for the current terminal/profile area but do not yet justify changing broad repo instructions.

## Candidates logged but not promoted

- Candidate: after upstream profile-model merges, keep built-in shell profiles compatible by preserving `['-l']` defaults and normalizing older profile documents.
- Reason not promoted: observed in one conflict-resolution path.
- Evidence: conflict repair passed `git ls-files -u`, targeted profile tests, and `flutter analyze`.

- Candidate: when adding new terminal runtime events, update `switch` handling in the example controller so Dart exhaustiveness checks continue to pass.
- Reason not promoted: normal compiler-enforced maintenance rule, not a durable repo instruction yet.
- Evidence: `TerminalSessionShellHookEvent` needed explicit handling after the merge.

- Candidate: terminal themes should model `4` special colors plus ANSI `16`; do not generate the palette from one main color, and do not expand to full xterm `16..255` unless there is a concrete need.
- Reason not promoted: implemented in the feature branch, but still a design decision specific to the current theme work.
- Evidence: the final implementation used `special + normal + bright` palette groups and runtime fill-in before native handoff.

- Candidate: when changing terminal colors, verify both Dart and native paths because runtime session creation, resize rebuilds, and frame export all need consistent palette application.
- Reason not promoted: logged as feature-specific verification context.
- Evidence: implementation touched Dart config/runtime, Rust model/session code, and example editor/tests.

- Candidate: local GitHub push can fail even after local commits if HTTPS or SSH credentials are unavailable.
- Reason not promoted: environment-specific, not a project convention.
- Evidence: push failed with missing HTTPS username and SSH public-key denial.

## Conflicts

- Conflict: none
- Existing guidance:
- Candidate guidance:
- Recommended resolution:

## Skipped sensitive or unsafe items

- Item category: none identified for durable storage
- Reason skipped:

## Validation

- Commands run: daily-learning redaction helper on this log; `git diff --check`; `rg -n '[ \t]+$' <changed-files>`
- Result: no redaction needed; `git diff --check` produced no output; trailing-whitespace scan found no matches.
