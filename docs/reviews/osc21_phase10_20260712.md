# Phase 10 review — Kitty OSC 21 and OSC 23 — 2026-07-12

## Outcome

- Start SHA: `a418f51ad85e0f8d6762bc261236490539af3a07`.
- Implementation SHA: `f7d48bbda8dbffab0fe637c0b1a306bbd9c4a761`.
- Branch: `codex/osc21-color-control-20260712`.
- Scope: bounded Kitty OSC 21 color control and removal of the non-standard
  OSC 23 title-stack side effect.

OSC 21 is no longer a generic no-op. Ianvs now supports ordered multi-field
set/query/reset for the full ANSI palette and core special colors, retains all
specified special-color slots, preserves profile baselines and exposes visible
values through the existing frame/render pipeline.

## Findings and resolution

| Finding | Resolution |
|---|---|
| OSC 21 was discarded despite a published Kitty specification | Added a dedicated appearance parser and state model |
| Palette/default/cursor already existed but could not be controlled together | Added ordered batch set/query/reset across shared runtime values |
| Special dynamic/alpha fields had no state | Added bounded snapshot-aware special-color and alpha state |
| Profile reset could be lost | OSC 21 mutations preserve immutable profile baselines and RIS restores them |
| VTE silently drops a 16th field | Documented and tested the 15-field ceiling plus next-sequence recovery |
| OSC 23 popped the CSI title stack | Removed the incorrect dispatch; OSC 23 is now a no-op |
| Unsupported renderer effects might be overclaimed | Selection text, visual-bell, cursor-text and transparency remain stored/queryable but non-visual |

## Focused evidence

- vendor parser: batch ordering, known/unknown query, color formats, alpha,
  dynamic values, malformed isolation, reset, byte splits, BEL/ST, 15-field
  ceiling, snapshot and RIS;
- native real PTY: combined query response, default/background/cursor frame
  colors, index 196 JSON/Protobuf parity, and VT220 timeout;
- shared corpus: 18 mirrored cases and 20 required edge classes, including
  OSC 21 application and OSC 23 title preservation;
- semantic probe: 14 deterministic intents, including OSC 21 and OSC 23;
- macOS real PTY: visible frame colors and stable OSC 23 title.

## Final verification

The final repository gate passed with exit code 0:

- corpus validator: 18 cases and 20 required edge classes;
- semantic probe: 14 deterministic intents;
- vendored terminal: 1,598 passed and 1 ignored; doc tests 11 passed and
  1 ignored;
- native core: lib 73/73, corpus 1/1, real-PTY/session 453/453 and vttest 3/3;
- terminal package: 441 passed; example grouped tests 913/913 and complete
  Widget tests 125/125;
- macOS smoke 4/4, real PTY 19/19 and native RunnerTests 10/10.

Final-tree command:

```bash
VERIFY_FLUTTER_TERMINAL_RUN_EXAMPLE_WIDGET_TESTS=1 \
  ./tools/verify_flutter_terminal.sh
```

The Computer Use gate then passed on a clean standalone cold launch. The app
rendered a live shell, opened the command menu and Profiles, created a second
Local Shell tab, executed `echo GATEOK`, displayed `GATEOK`, and reported
`SHELL ACTIVE` through accessibility semantics. No issue remained after the
automated and visible acceptance passes.

## Compatibility and rollback

No JSON or protobuf field was added: OSC 21 uses existing palette/default/cursor
frame contracts. OSC 4/10/11/12 and CSI title-stack behavior remain compatible.
Reverting this phase returns OSC 21 to a bounded no-op and restores the obsolete
OSC 23 bug; therefore OSC 23 should remain covered during any rollback review.
