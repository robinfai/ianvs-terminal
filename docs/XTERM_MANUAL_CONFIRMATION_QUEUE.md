# xterm.js Manual Confirmation Queue

Purpose: collect xterm.js audit risks that cannot be fully confirmed by local automated probes alone. Process this queue only after automated/source-confirmable rows in `docs/TERMINAL_XTERM_RECENT_FIX_AUDIT.md` and T-069 through T-076 have been updated with their latest evidence.

## Rules

- Do not change product code from this queue alone.
- Record actual observations before creating implementation work.
- If a manual probe confirms a product gap, create a smaller follow-up task card under `docs/tasks/` and link it from the audit.
- If a manual probe passes, update the audit row with the date, platform, command or steps, and mark the row `Covered` or `Likely covered` as appropriate.

## Queue

| ID | Audit row / task | Risk source | Impact | Manual steps | Expected result | Actual result | Blocker / reason automation is insufficient | Next action after confirmation |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| <a id="m-001"></a>M-001 | Alt-buffer wheel containment, [T-071](tasks/terminal-interaction/T-071-xterm-scroll-resize-alt-buffer-probes.md) | xterm.js #5437 | Wheel input may scroll the host page instead of the alternate-buffer app | Open `less` or `vim` in the example app, scroll with trackpad and mouse wheel while alternate screen is active | Alternate-buffer app consumes scroll; host/page does not move | Covered 2026-05-30 by widget regression: `TerminalViewport` claims pointer-scroll signals through Flutter's pointer signal resolver before parent scrollables can consume them. The focused test nests the terminal in a parent `SingleChildScrollView`, sends an alternate-screen wheel event, verifies parent scroll offset stays 0, and confirms the terminal emits alternate-scroll arrow bytes. Earlier Computer Use on 2026-05-29 could page `less` by keyboard but could not deliver scroll gestures to `less`. | Host manual scroll remains useful as smoke coverage, but parent-scroll containment is now pinned automatically. | Keep the widget regression; repeat manual smoke only if platform scroll delivery changes. |
| <a id="m-002"></a>M-002 | Alt-buffer scrollbar transition, [T-071](tasks/terminal-interaction/T-071-xterm-scroll-resize-alt-buffer-probes.md) | xterm.js #5411 | Scrollbar may jump when entering/exiting alternate screen | Enter alternate screen, scroll or drag scrollbar, exit to main buffer | Main buffer scroll position returns without teleporting or stale thumb position | Covered 2026-05-29 macOS 15.7.7 with Computer Use: generated main-buffer scrollback, entered `less`, paged from rows 1-19 to rows 20-38, observed no host scrollbar/thumb while the alternate screen was active, exited with `q`, and saw main-buffer rows `main-067` through `main-080` plus the prompt return with the host scrollbar at the bottom and no visible content teleport. Wrapped alt-buffer line-end metadata is covered separately by `cargo test --manifest-path native/core/Cargo.toml --test session_test session_preserves_wrapped_line_metadata_in_alternate_screen`. | Resolved for the current product behavior; alternate screen does not expose a draggable host thumb. | None unless a future UI exposes alternate-screen host scrolling. |
| <a id="m-003"></a>M-003 | Vertical-resize selection invalidation, [T-071](tasks/terminal-interaction/T-071-xterm-scroll-resize-alt-buffer-probes.md) | xterm.js #5423 | Selection may survive after row geometry changes | Select a multi-row range, resize terminal height, inspect selection and copy text | Selection is cleared or remapped by an explicitly documented policy | Covered 2026-05-29 macOS 15.7.7 with Computer Use: selected a multi-row range, then zoomed the window from 93x22 to 203x44; selection stayed visually aligned/remapped with no adjacent text occlusion. The focused selection-controller regression now pins the stable-row remap/offscreen-empty policy. | Resolved for the current product policy. | Keep the regression; repeat manual smoke only if resize or selection ownership changes. |
| <a id="m-004"></a>M-004 | CSI `8;t` reflow after terminal resize request, [T-071](tasks/terminal-interaction/T-071-xterm-scroll-resize-alt-buffer-probes.md) | xterm.js #5324 | Resize request may emit an event without proving post-resize reflow | Run a probe that writes a long wrapped line, emits `CSI 8;rows;cols t`, then checks visual reflow | Resize event and subsequent frame agree on wrapped row geometry | Covered 2026-05-29 macOS 15.7.7 with Computer Use: emitted `CSI 8;20;60t` while printing wrapped text; app resized to 60x20, rows reflowed, and no crash was observed. Native resize-event and wrapped-line reflow fixtures cover the automated pieces. | Resolved for the current product behavior. | Keep native resize/reflow fixtures; repeat manual smoke only if app/window resize coupling changes. |
| <a id="m-005"></a>M-005 | Selection rounded-corner rendering, [T-075](tasks/terminal-interaction/T-075-rendering-glyph-dpr-probes.md) | xterm.js #4427 | Selection overlay may clip or overpaint at rounded corners | Capture screenshot of selected text at row starts/ends and corner cells | Rounded selection corners are visually clean and do not cover adjacent text | Covered 2026-05-29 macOS 15.7.7 with Computer Use: drag-selected multi-row text before resize; overlay edges and corners looked clean and did not cover adjacent text. The 2026-05-29 pixel fixture also samples selected-cell interior and adjacent cell edges to prove selection paint stays within selected cell bounds. | Resolved for the current Flutter Canvas selection paint model. | Keep the pixel regression; repeat visual smoke only if selection painting changes. |
| <a id="m-006"></a>M-006 | Fractional-DPR text blur, [T-075](tasks/terminal-interaction/T-075-rendering-glyph-dpr-probes.md) | xterm.js #5292 | Text may blur at 125% or 150% scale | On a display scaled to 125% or 150%, render dense terminal text and capture screenshots | Text remains snapped and legible; no systematic half-pixel blur | Blocked 2026-05-29: current Computer Use visual run used DPR 2.0, not a 125% or 150% fractional-scale display; no system display setting was changed. The 2026-05-30 widget regression `flutter test test/terminal/render_terminal_viewport_test.dart --plain-name "snaps baselines"` now explicitly covers text baseline, background span, and powerline rect snapping at DPR 1.25, 1.5, and 2.5. | Flutter widget tests can set DPR and prove snapping geometry, but cannot reliably prove host display sharpness | If failing, create a DPR snapping/rendering task |
| <a id="m-007"></a>M-007 | High-DPI powerline and open-screen glyph visuals, [T-075](tasks/terminal-interaction/T-075-rendering-glyph-dpr-probes.md) | xterm.js #4924 and #5296 | Special glyphs may have wrong width, fallback, or placement | Render powerline and upstream open-screen code points at high DPI | Glyphs occupy intended cells with no overlap, clipping, or fallback boxes | Covered 2026-05-29 macOS 15.7.7 DPR 2.0 with Computer Use: powerline, open-screen scan-line glyphs, wide CJK, and emoji rendered without fallback boxes or obvious overlap in the example app. The focused open-screen glyph regression verifies `U+23BA` through `U+23BD` occupy sequential single cells. | Resolved for the current glyph set; broader host font fallback remains normal visual smoke territory. | Keep glyph placement regressions; create a focused task only if a future font/fallback probe fails. |
| <a id="m-008"></a>M-008 | Default font weight and bold+faint visual weight, [T-075](tasks/terminal-interaction/T-075-rendering-glyph-dpr-probes.md) | xterm.js #5359 and #5370 | Canvas text may use wrong visual weight for default or bold+faint styles | Compare default, bold, faint, and bold+faint rows in the example app | Default appears normal weight; bold+faint remains visibly bold while dimmed | Covered 2026-05-29 macOS 15.7.7 with Computer Use: ANSI style probe showed normal default weight, visibly bold bold text, dim faint text, and bold+faint remained bold while dimmed. The focused renderer regression also asserts default `FontWeight.w400` and bold+dim `FontWeight.w700`. | Resolved for the current style mapping. | Keep the resolved-cell font-weight regression. |
| <a id="m-009"></a>M-009 | Windows Ctrl-letter and CapsLock behavior, [T-074](tasks/terminal-interaction/T-074-platform-input-ime-paste-probes.md) | xterm.js `a1fca4f`, `afc6f0b` | Windows/WSL control bytes may duplicate or change under CapsLock | On Windows, test Ctrl-letter combinations in PowerShell and WSL with CapsLock on/off | Each Ctrl-letter sends one expected C0 byte; CapsLock does not duplicate input | Blocked 2026-05-29: requires Windows/WSL and CapsLock state; current host is macOS. | Flutter widget tests on macOS cannot represent win32 keyboard mode semantics | If failing, create Windows keyboard mapping task |
| <a id="m-010"></a>M-010 | Ctrl-C in fish and kitty keyboard mode decision, [T-070](tasks/terminal-interaction/T-070-kitty-keyboard-protocol-scope.md), [T-074](tasks/terminal-interaction/T-074-platform-input-ime-paste-probes.md) | xterm.js #5600 and `f5e81af` | Ctrl-C may fail under fish or after kitty protocol negotiation | In fish, press Ctrl-C with normal mode; if kitty support is accepted later, repeat with kitty mode enabled | Ctrl-C interrupts shell reliably; kitty scope decision is documented before implementation | 2026-05-29 macOS 15.7.7 with Computer Use: in fish normal mode, `sleep 10` was interrupted by ETX; terminal showed `^C` and `[SIGINT]`. Kitty mode remains deferred pending T-070/M-010 scope decision. | ianvs terminal currently has no kitty keyboard mode, so full parity requires product-scope confirmation | If kitty is accepted, create protocol implementation task; otherwise mark deferred |
| <a id="m-011"></a>M-011 | RTL native IME composition, [T-074](tasks/terminal-interaction/T-074-platform-input-ime-paste-probes.md) | xterm.js `2285ac6` | RTL text can break native IME composition rendering | With Chinese/Japanese IME or another native IME that supports RTL text, compose RTL text in the terminal | Composition stays attached to caret and commits correct RTL text | Covered 2026-05-30 by widget regression: `flutter test test/terminal_input_controller_test.dart --plain-name "RTL IME"` injects a Hebrew active composing range, verifies the composing overlay remains visible inside terminal bounds, and confirms commit writes the expected RTL UTF-8 bytes. Mid-text helper commit extraction, high-DPI/resize geometry sync, and long composition clipping are covered by separate focused IME tests. | Native compositor smoke remains useful after platform input changes, but the product rendering/commit path is pinned automatically. | Keep the RTL IME regression; repeat native smoke only if Flutter text input/compositor behavior changes. |
| <a id="m-012"></a>M-012 | Android physical keyboard duplication, [T-074](tasks/terminal-interaction/T-074-platform-input-ime-paste-probes.md) | xterm.js `d034164` | Android hardware keyboard events may duplicate input | On Android with physical keyboard, type repeated letters and shortcuts | One logical input per key event; repeats only when OS sends repeats | Blocked 2026-05-29: requires Android with physical keyboard; current host is macOS. | macOS/Linux widget tests cannot validate Android hardware-key delivery | If failing, create Android keyboard event de-duplication task |
| <a id="m-013"></a>M-013 | Release performance baseline, [T-076](tasks/verification-gates/T-076-terminal-performance-baseline-xterm-probes.md) | xterm.js #5184, #5440, #5864, #5825, #5829, #5830 | Local debug tests do not prove release performance on a quiet host | Run `tools/cat_log_benchmark.sh --out-dir <abs-dir> --profile release --include-raw-frames` on a quiet machine and record CPU/OS/Flutter/core profile | Baseline metrics are captured with host context and no suspicious outliers | 2026-05-29 and 2026-05-30 macOS 15.7.7 Apple M1 Pro 32 GiB: first release snapshots now cover bulk-output, input-echo, URL-heavy, Unicode-heavy, paste-throughput, search-extraction, and parser-heavy scenarios. Latest added evidence: paste-throughput at `/tmp/ianvs-terminal-xterm-paste-throughput-20260530` used a 45,954-byte, 640-line payload and observed visible sentinel echo in 107,939us; search-extraction at `/tmp/ianvs-terminal-xterm-search-extraction-20260530` returned 4,200 matches in a 371,497-byte result with p95 request time 27,785us; parser-heavy at `/tmp/ianvs-terminal-xterm-parser-heavy-20260530` used a 545,160-byte dense CSI/SGR/OSC 8 fixture with `terminal_process_micros` 66637. All latest trace/metrics/time JSON files validate with `python3 -m json.tool`. Host was not fully quiet, so M-013 remains only as a future quiet-host refresh. | Performance numbers depend on host load and hardware; current snapshots are sufficient first baselines but not quiet-host guarantees | Repeat the benchmark suite on a quiet host before comparing future optimization work; create focused parser/frame/search/paste tasks only if suspicious outliers appear |
