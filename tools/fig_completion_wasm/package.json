{
  "name": "ianvs-fig-completion-wasm",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build:wasm": "cargo build --manifest-path rust/Cargo.toml --release --target wasm32-unknown-unknown && mkdir -p dist && cp rust/target/wasm32-unknown-unknown/release/ianvs_fig_completion_wasm.wasm dist/fig_completion_core.wasm",
    "start": "node src/server.mjs",
    "test": "npm run build:wasm && node --test test/*.test.mjs"
  },
  "engines": {
    "node": ">=20"
  }
}
